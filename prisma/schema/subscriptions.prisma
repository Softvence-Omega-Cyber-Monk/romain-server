enum SubscriptionStatus {
  ACTIVE
  UNSUBSCRIBED
}

model SubscriptionEmail {
  id     String             @id @default(uuid())
  email  String             @unique
  status SubscriptionStatus @default(ACTIVE)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("subscription_emails")
}
